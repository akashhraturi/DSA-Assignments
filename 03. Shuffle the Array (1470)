class Solution {
public:
    vector<int> shuffle(vector<int>& nums, int n) {
        vector<int> vi;
        int j=n;
        for(int i=0; i<n; i++) {
            vi.push_back(nums[i]);
            vi.push_back(nums[j]);
            j++;
        }
        return vi;
    }
};
