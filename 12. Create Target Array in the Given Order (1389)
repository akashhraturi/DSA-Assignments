class Solution {
public:
    vector<int> createTargetArray(vector<int>& nums, vector<int>& index) {
        vector<int> vi;
        for(int i=0; i!=nums.size(); i++) {
            vi.insert(vi.begin() + index[i], nums[i]);
        }
        return vi;
    }
};
